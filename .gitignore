# Dependencies
node_modules/
.pnpm-store/
.pnpm-debug.log

# Prisma
.prisma/
packages/database/node_modules/

# Environment
.env
.env.local
.env.test

# Build outputs
dist/
build/
startup-error.log

tmp/

# Regenerated test artifact (permanent log lives in FINDINGS.md)
apps/api/tests/api/API_CERTIFICATION_REPORT.md

# Database dumps — never commit real backup files
# backup.sql, schema_dump*.sql already in repo as assessment artifacts
# but prevent future real dumps from being committed
*.dump
*.pgdump
*_backup_*.sql
*_dump_*.sql
