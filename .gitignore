# Dependencies
node_modules/
.pnpm-store/
.pnpm-debug.log

# Prisma
.prisma/
packages/database/node_modules/

# Environment
.env
.env.local

# Build outputs
dist/
build/
startup-error.log

tmp/

# Database dumps — never commit real backup files
# backup.sql, schema_dump*.sql already in repo as assessment artifacts
# but prevent future real dumps from being committed
*.dump
*.pgdump
*_backup_*.sql
*_dump_*.sql
